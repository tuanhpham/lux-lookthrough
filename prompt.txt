I want to extend my existing Python stock screener project.

IMPORTANT:

* DO NOT rewrite or refactor existing architecture
* DO NOT modify working VCP, EP, or breakout logic
* ONLY add new modules and integrate them minimally
* Keep full backward compatibility

---

# FEATURE 1 — MOMENTUM ENGINE (CORE ADDITION)

Create:

indicators/momentum.py

class MomentumEngine:

```
def compute_returns(self, price_series):
    - 1M return
    - 3M return
    - 6M return
    - 12M return

def compute_relative_strength(self, stock, benchmark="SPY"):
    - RS score normalized 0–100

def compute_momentum_score(self, stock):
    Weighted score:
    - 1M: 15
    - 3M: 25
    - 6M: 25
    - RS vs SPY: 25
    - Liquidity: 10
```

Return:

* momentum_score
* percentile_rank
* classification: (Weak / Building / Strong / Explosive)

---

# FEATURE 2 — MARKET REGIME FILTER

Create:

indicators/market_regime.py

Detect overall market condition using SPY / QQQ:

Define regime:

1. BULL TREND

   * SPY > EMA50 > EMA150 > EMA200
   * EMA200 rising

2. TRANSITION / CHOPPY

   * mixed EMA structure

3. BEAR / DEFENSIVE

   * SPY below EMA200

Output:

* regime_type
* regime_strength_score (0–100)
* risk_on / risk_off flag

---

# FEATURE 3 — SECTOR MOMENTUM ROTATION

Create:

indicators/sector_momentum.py

Compute relative strength per sector:

* Technology
* Healthcare
* Energy
* Financials
* Consumer
* Industrial
* Crypto-related equities (if applicable)

For each sector:

* avg 1M return
* avg 3M return
* RS vs SPY
* number of stocks in top 10% momentum

Output:

* sector_rankings
* hot_sectors (top 2–3)
* cold_sectors

---

# FEATURE 4 — VCP PRE-FILTER CHANGE (IMPORTANT)

Modify ONLY entry pipeline of VCP scanner.

Current behavior:

* likely uses 52-week high or full universe

NEW behavior:

Step 1:
Run MomentumEngine.rank_momentum()

Step 2:
Filter universe to top 10–20% momentum stocks ONLY

Step 3:
Optionally intersect with:

* hot sectors from SectorMomentumEngine

Step 4:
Pass result into existing VCP scanner unchanged

DO NOT modify VCP detection logic itself.

---

# FEATURE 5 — EXPLORATION / MANUAL SCAN MODE

Add a new CLI command:

--scan-momentum

Purpose:

Return stocks that are:

* Strong 1M / 3M / 6M movers
* NOT necessarily in VCP yet
* Near highs OR mid-trend OR breakout candidates
* High relative strength stocks

This mode is for:

"show me what is running right now so I can manually inspect charts"

Output:

Top 50 stocks sorted by momentum_score

Include:

* ticker
* momentum_score
* 1M / 3M / 6M returns
* RS score
* sector
* distance from 52-week high (%)
* volatility (ATR%)

This is NOT a VCP scan.

---

# FEATURE 6 — ENHANCED REPORT OUTPUT

Add new columns across reports:

* momentum_score
* momentum_percentile
* market_regime
* sector_rank
* sector_strength
* is_hot_sector

---

# FEATURE 7 — INTEGRATION RULES

* Do NOT break existing CLI commands
* Do NOT remove or replace existing filters
* All new modules must be OPTIONAL add-ons
* Existing VCP / EP / breakout scanners must still work standalone

---

# DESIGN PRINCIPLE

The system should now behave like:

1. Market Regime Filter (WHEN to be aggressive)
2. Sector Rotation Filter (WHERE money flows)
3. Momentum Engine (WHAT stocks are strong)
4. VCP / EP / Breakout Scanner (WHEN to enter)

---

# ACCEPTANCE CRITERIA

* Existing scans still work unchanged
* New momentum scan works independently (--scan-momentum)
* VCP scan now uses momentum-filtered universe
* Sector rotation influences selection
* No breaking changes
* No refactor of old code

---

# GOAL

Transform the screener from:

"pattern-based scanner"

to:

"multi-layer momentum + regime + sector + pattern system similar to institutional workflow"
